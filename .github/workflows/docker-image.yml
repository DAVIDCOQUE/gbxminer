name: Docker Image CI

on:
  push:
  pull_request:

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v8
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag gbxminer:latest --no-cache
    - name: Docker image list
      run: docker images
